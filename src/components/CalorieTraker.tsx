import { Activity } from "../types"

type CalorieTrakerProps = {
    activities: Activity[]
}

export default function CalorieTraker({ activities }: CalorieTrakerProps) {
    return (
        <div>CalorieTraker</div>
    )
}
